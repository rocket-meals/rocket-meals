/**
 * Translation keys used exclusively by common-ui components.
 *
 * Keys that are only relevant to app-specific screens should live in the
 * respective app's own locales (e.g. `apps/frontend/app/locales/keys.ts`).
 */
export enum CommonUITranslationKeys {
	ok = 'ok',
	cancel = 'cancel',
	confirm = 'confirm',
	close = 'close',
	loading = 'loading',
	error = 'error',
	save = 'save',
	delete = 'delete',
}
