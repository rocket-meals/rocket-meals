import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  label: {
    marginBottom: 5,
    textAlign: 'center',
    fontFamily: 'Poppins_400Regular',
    fontSize: 16,
  },
});
